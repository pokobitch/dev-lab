name: docker-build

on:
  push:
    branches:
      - main # mainブランチにpushされたときに実行

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Docker build
        run: |
          docker build -t scraping-news .